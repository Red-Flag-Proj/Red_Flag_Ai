아래가 **보고서/발표용으로 바로 넣을 수 있는 최종 개선안**입니다.

---

# 금융권 FDS 및 머신러닝 기반 이상거래 탐지 개선안

## 1. 문제 정의

본 프로젝트는 카드 사용내역을 기반으로 이상거래를 탐지하는 머신러닝 기반 FDS를 구축하는 것을 목표로 한다. 현재 데이터는 전체 약 9,000건 중 이상거래가 60건으로, 이상거래 비율이 매우 낮은 불균형 데이터 구조를 가진다.

기존 FDS는 주로 거래 금액, 시간, 위치, 가맹점, 해외 결제 여부 등 단일 거래의 조건을 기준으로 이상거래를 탐지한다. 그러나 실제 금융 환경에서는 사기 거래가 정상 거래처럼 위장되는 경우가 많고, 고객의 소비 패턴도 시간에 따라 변화하기 때문에 단순 룰 기반 탐지나 일반적인 지도학습 모델만으로는 한계가 있다.

따라서 본 프로젝트는 기존 FDS가 놓치기 쉬운 **정상 위장형 이상거래**, **소액 테스트 후 고액 결제**, **짧은 시간 내 반복 거래**, **사용자별 소비 패턴에서 벗어난 거래**를 탐지하기 위해 하이브리드 이상거래 탐지 모델을 설계한다.

---

# 2. 기존 금융권 FDS / ML 기반 탐지의 핵심 문제점

## 1) 극단적인 클래스 불균형

카드 거래 데이터는 대부분 정상거래이고, 이상거래는 매우 적다. 현재 데이터 역시 전체 약 9,000건 중 이상거래가 60건으로, 이상거래 비율은 약 0.67%에 불과하다.

이 경우 모델이 대부분의 거래를 정상으로 예측해도 정확도는 높게 나올 수 있다. 그러나 실제로 중요한 것은 전체 정확도가 아니라 이상거래를 얼마나 놓치지 않고 탐지하는지이다.

따라서 본 프로젝트에서는 accuracy보다 다음 지표를 중심으로 평가한다.

| 지표             | 의미                        |
| -------------- | ------------------------- |
| Recall         | 실제 이상거래 중 탐지한 비율          |
| Precision      | 이상거래로 탐지한 거래 중 실제 이상거래 비율 |
| F1-score       | Precision과 Recall의 균형     |
| PR-AUC         | 불균형 데이터에서의 탐지 성능          |
| False Positive | 정상거래를 이상거래로 잘못 탐지한 수      |
| False Negative | 이상거래를 정상으로 놓친 수           |

---

## 2) 단일 거래 중심 탐지의 한계

기존 FDS는 거래 1건의 금액, 시간, 국가, 가맹점 등을 기준으로 판단하는 경우가 많다.

예를 들어 다음 거래들은 각각만 보면 정상처럼 보일 수 있다.

```text
1차 거래: 3,000원
2차 거래: 9,000원
3차 거래: 120,000원
```

하지만 거래 흐름으로 보면 **소액 테스트 결제 후 고액 결제** 패턴일 수 있다.

따라서 단일 거래 기준이 아니라, 직전 거래와 최근 거래 흐름을 함께 분석해야 한다.

추가해야 할 조건은 다음과 같다.

```text
직전 거래와의 시간 차이
직전 거래 대비 금액 증가율
최근 10분 거래 횟수
최근 1시간 거래 총액
최근 3건의 금액 증가 패턴
소액 테스트 후 고액 결제 여부
짧은 시간 내 여러 가맹점 결제 여부
```

---

## 3) 개념 드리프트

사기 수법은 계속 변화하고, 고객의 소비 패턴도 시간이 지나면서 바뀐다. 과거 데이터로 학습한 모델이 현재의 새로운 사기 패턴을 제대로 탐지하지 못할 수 있다.

예를 들어 과거에는 고액 해외 결제가 주요 이상거래였다면, 최근에는 정상 시간대에 소액 결제를 여러 번 나누어 수행하는 방식이 사용될 수 있다.

따라서 모델은 고정된 기준만 사용해서는 안 되며, 최근 소비 패턴을 반영해야 한다.

추가해야 할 조건은 다음과 같다.

```text
최근 7일 평균 결제금액
최근 30일 평균 결제금액
최근 30일 대비 현재 거래 금액 비율
최근 거래 빈도 변화
사용자별 자주 쓰는 시간대 변화
사용자별 threshold
```

---

## 4) 라벨링 한계

실제 금융권에서는 모든 거래에 대해 즉시 정상/이상 라벨을 붙이기 어렵다. 이상거래 여부는 조사, 고객 확인, 신고 처리 등을 거쳐 확정되는 경우가 많기 때문이다.

현재 프로젝트도 이상거래 라벨이 60건뿐이므로 지도학습 모델만으로는 충분한 이상 패턴을 학습하기 어렵다.

따라서 지도학습 모델만 사용하는 것이 아니라 다음 요소를 함께 사용해야 한다.

```text
Rule Score
+ Supervised ML Score
+ Anomaly Detection Score
+ Personal Pattern Score
```

즉, 라벨이 적은 상황을 보완하기 위해 룰 기반 탐지와 비지도 이상탐지 모델을 함께 사용하는 하이브리드 구조가 필요하다.

---

## 5) 정상거래와 이상거래의 경계 모호성

이상거래는 항상 명확하게 이상하지 않다. 사기범은 정상 사용자처럼 행동하려고 하고, 반대로 정상 사용자도 여행, 이사, 고가 물품 구매 등으로 평소와 다른 소비를 할 수 있다.

예를 들어 해외 결제라고 해서 모두 이상거래는 아니다. 해외여행 중인 사용자의 해외 결제는 정상일 수 있다. 반대로 국내 결제라도 사용자의 평소 패턴과 크게 다르면 이상거래일 수 있다.

따라서 단순히 “해외 결제”, “고액 결제”, “새벽 결제” 여부만 볼 것이 아니라, 해당 거래가 **그 사용자에게 얼마나 낯선 거래인지**를 판단해야 한다.

추가해야 할 조건은 다음과 같다.

```text
사용자 평균 대비 금액 이탈
사용자 평소 시간대와의 차이
사용자에게 처음 나타난 국가
사용자에게 처음 나타난 가맹점
사용자에게 처음 나타난 업종
개인별 위험 점수 threshold
```

---

# 3. 최종 개선 방향

본 프로젝트의 개선 방향은 다음과 같다.

> 기존 FDS가 “이 거래가 일반적으로 이상한가?”를 판단했다면,
> 본 프로젝트는 “이 거래가 해당 사용자에게 이상한가?”와 “최근 거래 흐름상 이상한 변화가 있는가?”를 함께 판단한다.

이를 위해 기존 하이브리드 FDS에 다음 4가지 요소를 추가한다.

```text
1. 개인별 소비 패턴 기반 feature
2. 직전 거래 대비 변화량 feature
3. 최근 거래 흐름 기반 feature
4. 비지도 이상탐지 기반 anomaly score
```

---

# 4. 최종 가설

## 가설 1. 거래 흐름 기반 feature를 추가하면 기존 FDS가 놓치는 정상 위장형 이상거래를 탐지할 수 있다.

기존 모델은 거래 1건 단위로 이상 여부를 판단하기 때문에, 소액 테스트 후 고액 결제나 짧은 시간 내 반복 결제 같은 흐름을 놓칠 수 있다.

따라서 다음 feature를 추가한다.

```text
time_since_prev_tx
amount_ratio_to_prev_tx
tx_count_last_10min
tx_count_last_1h
amount_sum_last_1h
small_test_then_large_tx
amount_increasing_pattern
unique_merchant_count_last_1h
```

기대 효과는 다음과 같다.

```text
소액 테스트 후 고액 결제 탐지
짧은 시간 내 반복 결제 탐지
금액이 점진적으로 증가하는 이상 패턴 탐지
여러 가맹점에 분산된 이상 결제 탐지
```

---

## 가설 2. 사용자별 소비 패턴 이탈 점수를 추가하면 오탐을 줄일 수 있다.

같은 50만 원 결제라도 사용자에 따라 의미가 다르다. 평소 5천 원~2만 원을 쓰는 사용자에게는 이상거래일 수 있지만, 평소 고액 결제를 자주 하는 사용자에게는 정상거래일 수 있다.

따라서 전체 공통 기준이 아니라 개인별 기준을 반영한다.

추가 feature는 다음과 같다.

```text
user_avg_amount_30d
user_median_amount_30d
user_std_amount_30d
amount_ratio_to_user_avg_30d
amount_z_score_by_user
hour_not_in_user_common_hours
new_country_for_user
new_merchant_for_user
new_category_for_user
user_foreign_tx_ratio
user_night_tx_ratio
```

기대 효과는 다음과 같다.

```text
개인별 소비 패턴을 반영한 이상거래 탐지
해외 결제 이력이 많은 사용자의 오탐 감소
평소 소액 소비자의 고액 이상거래 탐지 강화
사용자별 threshold 적용을 통한 정밀도 향상
```

---

## 가설 3. 룰 기반 탐지, 지도학습 모델, 비지도 이상탐지를 결합하면 새로운 이상거래 유형에 더 강하다.

지도학습 모델은 과거에 라벨링된 이상거래와 유사한 패턴을 잘 탐지한다. 하지만 새로운 사기 유형이나 라벨이 부족한 이상 패턴에는 약할 수 있다.

따라서 최종 모델은 다음과 같은 하이브리드 구조로 설계한다.

```text
Final Risk Score
= Rule Score
+ ML Score
+ Personal Pattern Score
+ Sequence Pattern Score
+ Anomaly Score
```

각 점수의 역할은 다음과 같다.

| 점수                     | 역할                       |
| ---------------------- | ------------------------ |
| Rule Score             | 명확한 위험 조건 탐지             |
| ML Score               | 과거 라벨 기반 이상거래 예측         |
| Personal Pattern Score | 사용자 개인 패턴 이탈 탐지          |
| Sequence Pattern Score | 최근 거래 흐름 이상 탐지           |
| Anomaly Score          | 정상 분포에서 벗어난 새로운 이상 패턴 탐지 |

---

# 5. 기존 모델에서 확장해야 할 조건

## 1) 기존 모델 조건

기존 모델은 주로 다음 조건을 사용한다.

```text
amount
transaction_time
hour
is_night_transaction
country
is_foreign_transaction
merchant_category
payment_method
```

이 조건만으로는 단일 거래의 기본적인 이상 여부는 판단할 수 있지만, 개인별 패턴이나 최근 거래 흐름을 반영하기 어렵다.

---

## 2) 확장 모델 조건

확장 모델에서는 다음 조건을 추가한다.

### 개인 금액 기준 feature

```text
user_avg_amount_7d
user_avg_amount_30d
user_median_amount_30d
user_std_amount_30d
amount_ratio_to_user_avg_30d
amount_ratio_to_user_median_30d
amount_z_score_by_user
```

### 직전 거래 기준 feature

```text
prev_transaction_time
prev_amount
prev_country
prev_merchant
prev_merchant_category

time_since_prev_tx
amount_diff_from_prev_tx
amount_ratio_to_prev_tx
country_changed_from_prev_tx
merchant_changed_from_prev_tx
category_changed_from_prev_tx
```

### 최근 거래 흐름 feature

```text
tx_count_last_5min
tx_count_last_10min
tx_count_last_1h
tx_count_last_24h

amount_sum_last_10min
amount_sum_last_1h
amount_sum_last_24h

unique_merchant_count_last_1h
unique_category_count_last_1h
unique_country_count_last_24h
```

### 개인 패턴 이탈 feature

```text
new_country_for_user
new_merchant_for_user
new_category_for_user
hour_not_in_user_common_hours
is_unusual_payment_method
```

### 사기 시나리오 feature

```text
small_test_then_large_tx
amount_increasing_pattern
same_amount_repeated
rapid_multi_transaction
impossible_travel
```

---

# 6. 주요 탐지 조건 예시

| 탐지 조건                  | 설명             | 위험 점수 |
| ---------------------- | -------------- | ----: |
| 개인 평균 대비 3배 이상 고액      | 평소 소비보다 큰 금액   |   +20 |
| 개인 금액 z-score 3 이상     | 개인 소비 분포에서 벗어남 |   +25 |
| 최근 10분 내 3회 이상 거래      | 짧은 시간 반복 결제    |   +20 |
| 직전 거래 대비 금액 5배 이상 증가   | 급격한 금액 증가      |   +15 |
| 소액 테스트 후 고액 결제         | 사기 시나리오 가능성    |   +30 |
| 처음 보는 국가               | 사용자에게 낯선 국가    |   +20 |
| 처음 보는 가맹점 + 고액         | 신규 가맹점에서 고액 결제 |   +25 |
| 평소 사용하지 않는 시간대         | 개인 시간 패턴 이탈    |   +10 |
| 최근 1시간 총액이 평소 하루 평균 초과 | 단시간 과소비 패턴     |   +25 |
| 1시간 내 여러 가맹점 결제        | 분산 결제 패턴       |   +15 |

---

# 7. 최종 위험도 산출 방식

초기 모델에서는 사람이 정의한 가중치를 기반으로 시작하고, 이후 실험 결과에 따라 조정한다.

```text
Final Risk Score =
0.30 × Rule Score
+ 0.35 × ML Score
+ 0.20 × Personal Pattern Score
+ 0.10 × Sequence Pattern Score
+ 0.05 × Anomaly Score
```

위험도 구간은 다음과 같이 설정한다.

| 최종 Risk Score | 판단  | 조치                  |
| ------------: | --- | ------------------- |
|          0~39 | 정상  | 승인                  |
|         40~59 | 주의  | 모니터링                |
|         60~79 | 의심  | 추가 인증 / 관리자 확인      |
|        80~100 | 고위험 | 거래 보류 / 차단 / 관리자 검토 |

---

# 8. 모델 학습 시 개선 조건

현재 이상거래가 60건으로 매우 적기 때문에 학습 과정에서는 클래스 불균형 처리가 필요하다.

## 적용할 방법

```text
class_weight = "balanced"
scale_pos_weight = 정상거래 수 / 이상거래 수
threshold 조정
비지도 이상탐지 모델 보조 사용
```

현재 데이터 기준으로는 다음과 같다.

```text
scale_pos_weight = 8940 / 60 = 149
```

단, 이 값을 그대로 고정하기보다 50, 100, 149 등 여러 값을 실험하면서 오탐과 미탐의 균형을 확인한다.

---

# 9. 평가 기준

본 프로젝트에서는 accuracy를 핵심 지표로 사용하지 않는다. 이상거래 비율이 매우 낮기 때문에 accuracy는 실제 탐지 성능을 왜곡할 수 있다.

최종 평가는 다음 지표를 기준으로 한다.

| 평가 지표          | 목적                       |
| -------------- | ------------------------ |
| Recall         | 이상거래를 얼마나 놓치지 않는지 확인     |
| Precision      | 오탐이 얼마나 적은지 확인           |
| F1-score       | Recall과 Precision의 균형 확인 |
| PR-AUC         | 불균형 데이터에서 모델 성능 확인       |
| False Positive | 정상거래를 잘못 탐지한 수           |
| False Negative | 이상거래를 놓친 수               |

우선순위는 다음과 같다.

```text
1순위: 미탐 감소
2순위: 오탐 감소
3순위: F1-score 향상
4순위: 관리자 해석 가능성 확보
```

---

# 10. 관리자 대시보드 반영 방식

모델이 이상거래를 탐지했을 때, 단순히 “이상거래”라고 표시하는 것이 아니라 탐지 사유를 함께 제공해야 한다.

예시는 다음과 같다.

```text
위험도: 87점
판단: 고위험 이상거래 의심

탐지 사유:
1. 최근 10분 내 3회 이상 거래가 발생했습니다.
2. 직전 거래 대비 금액이 6.2배 증가했습니다.
3. 사용자 평균 결제금액보다 4.8배 큰 거래입니다.
4. 사용자가 평소 이용하지 않던 가맹점에서 결제되었습니다.
5. 최근 1시간 결제 총액이 평소 하루 평균 사용액을 초과했습니다.

추천 조치:
추가 인증 요청 또는 거래 보류 후 관리자 검토
```

이렇게 하면 모델 성능뿐 아니라 실제 금융권 FDS처럼 **탐지 근거 기반 의사결정**이 가능해진다.

---

# 11. 최종 보고서용 요약 문장

아래 문단을 최종본으로 사용하면 됩니다.

> 금융권 FDS와 머신러닝 기반 이상거래 탐지는 극단적인 클래스 불균형, 실시간 거래 흐름 반영의 어려움, 개념 드리프트, 라벨링 한계, 정상거래와 이상거래의 경계 모호성이라는 문제를 가진다. 특히 카드 거래 데이터는 대부분이 정상거래이고 이상거래는 매우 적기 때문에 정확도만으로는 모델 성능을 평가하기 어렵다. 또한 거래는 실시간으로 연속 발생하며, 사기 수법과 고객 소비 패턴이 계속 변화하기 때문에 고정된 룰이나 과거 데이터 기반 모델만으로는 정상거래처럼 위장된 이상거래를 탐지하기 어렵다.
>
> 이에 본 프로젝트는 기존 하이브리드 FDS 구조에 사용자별 소비 패턴, 최근 7일/30일 거래 흐름, 직전 거래 대비 변화량, 최근 거래 빈도, 비지도 이상탐지 점수를 추가한다. 최종 위험도는 룰 기반 점수, 지도학습 모델 점수, 개인 패턴 이탈 점수, 최근 거래 흐름 점수, 이상탐지 점수를 결합하여 산출한다. 이를 통해 단순 고액·해외·새벽 거래뿐 아니라 기존 방식이 놓치기 쉬운 소액 테스트 후 고액 결제, 짧은 시간 내 반복 결제, 사용자 패턴에서 벗어난 정상 위장형 이상거래를 탐지하는 것을 목표로 한다.

---

# 12. 최종 한 줄 컨셉

> 본 프로젝트는 기존 FDS의 단일 거래 중심 탐지 한계를 보완하기 위해, **개인별 소비 패턴과 최근 거래 흐름을 함께 분석하는 하이브리드 이상거래 탐지 모델**을 구축한다.
